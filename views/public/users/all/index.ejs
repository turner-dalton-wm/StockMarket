<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Stocks</title>
</head>
<body>
<h3>Users</h3>
<script type="text/javascript" src="../../js/dependencies/sails.io.js"></script>
<script type="text/javascript">
    var userName = location.href.substring(location.href.lastIndexOf('/') + 1);
    console.log(userName);
    io.socket.get('/user_all_connect/' + userName, function (data, jwr) {
        console.log('Server responded with status code ' + jwr.statusCode + ' and data: ', data);
    });
    io.socket.on('response', function (data) {
        console.log('NOW VIEWING ALL USERS');
    });
</script>
</body>
</html>